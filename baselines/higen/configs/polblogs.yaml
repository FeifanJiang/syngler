# HiGen on polblogs. Driver reads this YAML, then loads the full HiGen-native
# config at <SRC>/configs/higen_syngler_polblogs_<variant>.yaml.
dataset: polblogs
variant: single        # 'single' (paper default) or 'single_ml' (multi-level)
