# HiGen on yelp. Driver reads this YAML, then loads the full HiGen-native
# config at <SRC>/configs/higen_syngler_yelp_<variant>.yaml.
dataset: yelp
variant: single        # 'single' (paper default) or 'single_ml' (multi-level)
