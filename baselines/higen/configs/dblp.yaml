# HiGen on dblp. Driver reads this YAML, then loads the full HiGen-native
# config at <SRC>/configs/higen_syngler_dblp_<variant>.yaml.
dataset: dblp
variant: single        # 'single' (paper default) or 'single_ml' (multi-level)
