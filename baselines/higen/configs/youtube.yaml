# HiGen on youtube. Driver reads this YAML, then loads the full HiGen-native
# config at <SRC>/configs/higen_syngler_youtube_<variant>.yaml.
dataset: youtube
variant: single        # 'single' (paper default) or 'single_ml' (multi-level)
